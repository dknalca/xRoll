name: macOS

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-and-test:
    runs-on: macos-13
    steps:
      - uses: actions/checkout@v4
      - run: swift build
      - run: swift test
